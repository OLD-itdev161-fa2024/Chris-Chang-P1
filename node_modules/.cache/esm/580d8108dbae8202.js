let mongoose,config;_dba‍.x([["default",()=>_dba‍.o]]);_dba‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_dba‍.w("config",[["default",["config"],function(v){config=v}]]);


// Get the connection string
const db = config.get('mongoURI');

// Connect to MongoDB
const connectDatabase = async () => {
    try {
        await mongoose.connect(db, {
            useUnifiedTopology: true
        });
        console.log('Connected to MongoDB');
    } catch (error) {
        _dba‍.g.console.error(error.message);

        //exit with failure code
        process.exit(1);
    }
};

_dba‍.d(connectDatabase);